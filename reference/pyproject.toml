[build-system]
requires = ["setuptools"]

[project]
name = "pyRISM"
version = "0.2.0"
dependencies = [
    "toml",
    "numpy",
    "scipy",
    "pandas",
    "numba",
    "matplotlib",
    "click",
]

[tool.setuptools.packages]
find = {}

[project.scripts]
pyrismref = "pyrism.__main__:cli"
